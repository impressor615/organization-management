const TIMESTAMPS = {
  timestamps: {
    createdAt: 'created_at',
    updatedAt: 'updated_at',
  },
};

const AUTHORITY = {
  admin: 'admin',
  member: 'member',
};

const TEAM_SIZES = {
  tiny: 'tiny',
  small: 'small',
  medium: 'medium',
  big: 'big',
};

module.exports = {
  TIMESTAMPS,
  AUTHORITY,
  TEAM_SIZES,
};
