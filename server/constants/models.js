const TIMESTAMPS = {
  timestamps: {
    createdAt: 'created_at',
    updatedAt: 'updated_at',
  },
};

const AUTHORITY = {
  admin: 'admin',
  member: 'member',
};

module.exports = {
  TIMESTAMPS,
  AUTHORITY,
};
